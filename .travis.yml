language: node_js

node_js:
    - "node"
    - "12"

sudo: false

git:
    submodules: false

install:
  - npm install

before_script:
  - npm start &

script:
    - npm test

after_success:
    - npm run report-coveralls
    - npm run report-codecov